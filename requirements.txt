# Emotionally Adaptive Chatbot for Cyberbullying Support — direct dependencies
# Python 3.11. Versions pinned from the reference conda env (`chatbots`).
#
# NOTE on PyTorch: this project was developed with the CUDA 12.8 build
# (torch==2.11.0+cu128) on an NVIDIA RTX 5080. The line below pins the plain
# build for portability; for GPU acceleration install the matching CUDA wheel:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128

# --- Deep learning / NLP ---
torch==2.11.0
transformers==5.4.0
sentence-transformers==5.3.0
scikit-learn==1.8.0
scipy==1.17.1

# --- RAG: vector stores & retrieval ---
chromadb==1.5.5
faiss-cpu==1.13.2
rank-bm25==0.2.2

# --- LangChain (LCEL) ---
langchain-community==0.4.1
langchain-core==1.2.23
langchain-ollama==1.0.1
langchain-text-splitters==1.1.1
langchain-huggingface==1.2.1

# --- Local SLM inference ---
ollama==0.6.1

# --- LLM-as-a-judge (evaluation only) ---
anthropic==0.109.2

# --- Data & plotting ---
pandas==3.0.2
numpy==2.4.3
matplotlib==3.10.8
emoji==2.15.0
PyYAML==6.0.3

# --- Interface ---
gradio==6.11.0

# --- Testing ---
pytest==9.0.2
requests==2.33.1
